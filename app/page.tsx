import Banner from '@/components/blocks/Banner';

const HomePage = () => {
    return (
        <main className='relative w-full'>
            <Banner />
        </main>
    );
};

export default HomePage;
